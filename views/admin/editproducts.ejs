
<!DOCTYPE html>
<head>
<title>Admin Usermanagement</title>
<meta name="viewport" content="width=device-width, initial-scale=1">
<meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
<meta name="keywords" content="Visitors Responsive web template, Bootstrap Web Templates, Flat Web Templates, Android Compatible web template, 
Smartphone Compatible web template, free webdesigns for Nokia, Samsung, LG, SonyEricsson, Motorola web design" />
<script type="application/x-javascript"> addEventListener("load", function() { setTimeout(hideURLbar, 0); }, false); function hideURLbar(){ window.scrollTo(0,1); } </script>
<!-- bootstrap-css -->
<link rel="stylesheet" href="/admin/bootstrap.min.css" >
<!-- //bootstrap-css -->
<!-- Custom CSS -->
<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
<link href="/admin/style.css" rel='stylesheet' type='text/css' />
<link href="/admin/style-responsive.css" rel="stylesheet"/>
<!-- font CSS -->
<link href='//fonts.googleapis.com/css?family=Roboto:400,100,100italic,300,300italic,400italic,500,500italic,700,700italic,900,900italic' rel='stylesheet' type='text/css'>
<!-- font-awesome icons -->
<link rel="stylesheet" href="/admin/font.css" type="text/css"/>
<link href="/admin/font-awesome.css" rel="stylesheet"> 
<link rel="stylesheet" href="/admin/morris.css" type="text/css"/>


<!-- //font-awesome icons -->
<script src="/js/admin/jquery2.0.3.min.js"></script>
<script src="/js/admin/raphael-min.js"></script>
<script src="/js/admin/morris.js"></script>
<style>
    .error-message {
        color: red; /* Set error messages to red color */
        margin-top: 5px;
    }
</style>
</head>
<body>
<section id="container">
<!--header start-->
<header class="header fixed-top clearfix">
<!--logo start-->
<div class="brand">
    <a href="/admin" class="logo">
        ADMIN
    </a>
    <!-- <div class="sidebar-toggle-box">
        <div class="fa fa-bars"></div>
    </div> -->
</div>
<!--logo end-->

</div>
<div class="top-nav clearfix">
    <!--search & user info start-->
    <ul class="nav pull-right top-menu">
        <li>
            <input type="text" class="form-control search" placeholder=" Search">
        </li>
        <!-- user login dropdown start-->
        <li class="dropdown">
            <a data-toggle="dropdown" class="dropdown-toggle" href="#">
                <img alt="" src="images/2.png">
                <span class="username">John Doe</span>
                <b class="caret"></b>
            </a>
            <ul class="dropdown-menu extended logout">
                <li><a href="#"><i class=" fa fa-suitcase"></i>Profile</a></li>
                <li><a href="#"><i class="fa fa-cog"></i> Settings</a></li>
                <li><a href="login.html"><i class="fa fa-key"></i> Log Out</a></li>
            </ul>
        </li>
        <!-- user login dropdown end -->
       
    </ul>
    <!--search & user info end-->
</div>
</header>
<!--header end-->

<!--sidebar start-->
<aside>
    <div id="sidebar" class="nav-collapse">
        <!-- sidebar menu start-->
        <div class="leftside-navigation">
            <ul class="sidebar-menu" id="nav-accordion">
                <li>
                    <a class="active" href="/admin">
                        <i class="fas fa-tachometer-alt"></i>
                        <span>Dashboard</span>
                    </a>
                </li>
                <li class="sub-menu">
                    <a href="/admin/users">
                        <i class="fas fa-users"></i>
                        <span>Users</span>
                    </a>
                    
                </li>
                <li>
                    <a href="/admin/category">
                        <i class="fas fa-shopping-cart"></i>
                        <span>Category </span>
                    </a>
                </li>
                <li class="sub-menu">
                    <a href="/admin/products">
                        <i class="fas fa-box"></i>
                        <span>Products</span>
                    </a>
                   
                </li>
                <li class="sub-menu">
                    <a href="/admin/orders">
                        <i class="fas fa-shopping-bag"></i>
                        <span>Orders</span>
                    </a>
                   
                </li>
                <li class="sub-menu">
                    <a href="/admin/Coupon">
                        <i class="fas fa-shopping-bag"></i>
                        <span>Coupon</span>
                    </a>
                   
                </li>
                <li class="sub-menu">
                    <a href="/admin/return">
                        <i class="fas fa-undo"></i>
                        <span>Return Order</span>
                    </a>
                   
                </li>
                
            </ul>            </div>
        <!-- sidebar menu end-->
    </div>
</aside>
<!--sidebar end-->
<!--main content start-->

<section id="main-content">
    <section class="wrapper">
        <h1>Edit Products</h1>
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-md-6">
                    <form action="/admin/Products/update/<%=product._id%>" method="post" enctype="multipart/form-data" onsubmit="return validateForm()">
                        <div class="form-group">
                            <label for="productName"><i class="fas fa-user"></i> Name</label>
                            <input type="text" name="name" class="form-control" value="<%= product.name %>" id="productName" placeholder="Enter product name">
                        <div id="errorName" class="error-message"></div>
                            <% if (errorMessage) { %>
                                <p style="color: red;"><%= errorMessage %></p>
                            <% } %>
                        </div>
                        <div class="form-group">
                            <label for="productDescription"><i class="fas fa-file-alt"></i> Description</label>
                            <textarea class="form-control" name="description" id="productDescription"><%- product.description %></textarea>
                            <div id="errorDescription" class="error-message"></div>
                        </div>
                        <div class="form-group">
                            <label for="productPrice"><i class="fas fa-dollar-sign"></i> Price</label>
                            <input type="number" name="price" class="form-control" id="productPrice" value="<%= product.price %>" placeholder="Enter product price">
                            <div id="errorPrice" class="error-message"></div>
                        </div>
                        <div class="form-group">
                            <label for="productCategory"><i class="fas fa-tags"></i> Category</label>
                            <input type="text" name="category" class="form-control" id="productCategory" value="<%= product.category %>" placeholder="Enter product category">
                            <div id="errorCategory" class="error-message"></div>
                        </div>
                        <div class="form-group">
                            <label for="productImages"><i class="fas fa-image"></i> Image Update</label>
                            <input type="file" name="image" class="form-control-file" id="productImages" multiple>
                            <div id="errorImages" class="error-message"></div>
                        </div>
                        <div class="form-group">
                            <div>
                                <label>Current Images:</label>
                                <% product.image.forEach((img) => { %>
                                  <img src="/productimg/<%= img %>" width="100px" height="100px" alt="<%= img %>">
                                <% }) %>
                              </div>
                        </div>
                        <div class="form-group">
                            <label for="productStock"><i class="fas fa-cubes"></i> Stock</label>
                            <input type="number" name="stock" class="form-control" value="<%= product.stock %>" id="productStock" placeholder="Enter product stock">
                        <div id="errorStock" class="error-message"></div>
                        </div>
                        <div class="form-group">
                            <label for="productStock"><i class="fas fa-cubes"></i> Offer Price</label>
                            <input type="text" name="offerprice" class="form-control" value="<%=product.OfferPrice%>" id="productOfferPrice" placeholder="Enter product offer Price">
                        <div id="errorOfferPrice" class="error-message"></div>
                        </div>
                        <div class="form-group">
                            <label for="discount"><i class="fas fa-cubes"></i> Discount</label>
                            <input type="text" name="discount" class="form-control" id="productDiscount" value="<%=product.Discount%>" placeholder="Enter product Discount">
                        <div id="errorDiscount" class="error-message"></div>
                        </div>
                        <button type="submit" class="btn btn-primary"><i class="fas fa-check"></i> Submit</button>
                    </form>
                </div>
            </div>
        </div>
    </section>
</section>
<!-- <script>
    function validateForm() {
        resetErrorMessages();

        let isValid = true;

        // Validate Name
        const nameInput = document.getElementById('productName');
        const errorName = document.getElementById('errorName');
        if (nameInput.value.trim() === '') {
            errorName.textContent = 'Name is required';
            isValid = false;
        }

        // Validate Description
        const descriptionInput = document.getElementById('productDescription');
        const errorDescription = document.getElementById('errorDescription');
        if (descriptionInput.value.trim() === '') {
            errorDescription.textContent = 'Description is required';
            isValid = false;
        }

        // Validate Price
        const priceInput = document.getElementById('productPrice');
        const errorPrice = document.getElementById('errorPrice');
        // if (priceInput.value.trim() === '') {
        //     errorPrice.textContent = 'Price is required';
        //     isValid = false;
        // }
        if (isNaN(price) || price <= 0) {
        errorPrice.textContent = 'Price must be a valid number greater than zero';
        isValid = false;
    }

        // Validate Category
        const categoryInput = document.getElementById('productCategory');
        const errorCategory = document.getElementById('errorCategory');
        if (categoryInput.value.trim() === '') {
            errorCategory.textContent = 'Category is required';
            isValid = false;
        }

        // Validate Stock
        const stockInput = document.getElementById('productStock');
        const errorStock = document.getElementById('errorStock');
        // if (stockInput.value.trim() === '') {
        //     errorStock.textContent = 'Stock is required';
        //     isValid = false;
        // }
        if (isNaN(stock) || stock <= 0) {
        errorStock.textContent = 'Stock must be a valid number greater than zero';
        isValid = false;
    }

        // Validate Offer Price
        const offerPriceInput = document.getElementById('productOfferPrice');
        const errorOfferPrice = document.getElementById('errorOfferPrice');
        const offerPrice = parseFloat(offerPriceInput.value);
    if (isNaN(offerPrice) || offerPrice < 0) {
        errorOfferPrice.textContent = 'Offer Price must be a valid number greater than or equal to zero';
        isValid = false;
    }

        // Validate Discount
        const discountInput = document.getElementById('productDiscount');
        const errorDiscount = document.getElementById('errorDiscount');
        const discount = parseFloat(discountInput.value);
    if (isNaN(discount) || discount < 0) {
        errorDiscount.textContent = 'Discount must be a valid number greater than or equal to zero';
        isValid = false;
    }

        // Additional validations can be added for Offer Price and Discount

        if (!isValid) {
            // Prevent form submission if not valid
            return false;
        }

        return true;
    }

    // function resetErrorMessages() {
    //     const errorElements = document.querySelectorAll('.error-message');
    //     errorElements.forEach(element => {
    //         element.textContent = '';
    //     });
    // }
    function resetErrorMessages() {
    // Reset all error messages to empty
    document.getElementById('errorName').textContent = '';
    document.getElementById('errorDescription').textContent = '';
    document.getElementById('errorPrice').textContent = '';
    document.getElementById('errorCategory').textContent = '';
    document.getElementById('errorStock').textContent = '';
    document.getElementById('errorOfferPrice').textContent = '';
    document.getElementById('errorDiscount').textContent = '';
}
</script> -->

<script>
    function validateForm() {
    let isValid = true;

    const name = document.getElementById('productName').value;
    const description = document.getElementById('productDescription').value;
    const price = parseFloat(document.getElementById('productPrice').value);
    const category = document.getElementById('productCategory').value;
    const image = document.getElementById('productImages').value; // Corrected ID
    const stock = parseFloat(document.getElementById('productStock').value);
    const offerPrice = parseFloat(document.getElementById('productOfferPrice').value);
    const discount = parseFloat(document.getElementById('productDiscount').value);

    const errorName = document.getElementById('errorName');
    const errorDescription = document.getElementById('errorDescription');
    const errorPrice = document.getElementById('errorPrice');
    const errorImages = document.getElementById('errorImages'); // Corrected ID
    const errorStock = document.getElementById('errorStock');
    const errorOfferPrice = document.getElementById('errorOfferPrice');
    const errorDiscount = document.getElementById('errorDiscount');

    errorName.innerHTML = '';
    errorDescription.innerHTML = '';
    errorPrice.innerHTML = '';
    errorImages.innerHTML = ''; // Corrected ID
    errorStock.innerHTML = '';
    errorOfferPrice.innerHTML = '';
    errorDiscount.innerHTML = '';

    if (name.trim() === '') {
        errorName.innerHTML = 'Please enter a product Name.';
        isValid = false;
    }

    if (description.trim() === '') {
        errorDescription.innerHTML = 'Please enter a product description.';
        isValid = false;
    }

    if (isNaN(price) || price <= 0) {
        errorPrice.innerHTML = 'Price must be a number greater than zero.';
        isValid = false;
    }

    if (category.trim() === '') {
        // Add custom validation if required
    }

    if (!image) {
        errorImages.innerHTML = 'Please select an image.'; // Corrected ID
        isValid = false;
    }

    if (isNaN(stock) || stock <= 0) {
        errorStock.innerHTML = 'Stock must be a number greater than zero.';
        isValid = false;
    }

    if (isNaN(offerPrice) || offerPrice <= 0) {
        errorOfferPrice.innerHTML = 'Offer Price must be a number greater than zero.';
        isValid = false;
    }

    if (isNaN(discount) || discount < 0 || discount >= price) {
        errorDiscount.innerHTML = 'Discount must be a positive number less than the product price.';
        isValid = false;
    }

    return isValid;
}

      </script>






<!--main content end-->

 <script src="https://code.jquery.com/jquery-3.3.1.slim.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.14.7/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/js/bootstrap.min.js"></script>
<script src="js/admin/bootstrap.js"></script>
<script src="js/admin/jquery.dcjqaccordion.2.7.js"></script>
<script src="js/admin/scripts.js"></script>
<script src="js/admin/jquery.slimscroll.js"></script>
<script src="js/admin/jquery.nicescroll.js"></script>
<!--[if lte IE 8]><script language="javascript" type="text/javascript" src="js/flot-chart/excanvas.min.js"></script><![endif]-->
<script src="/js/admin/jquery.scrollTo.js"></script>
<!--     ///////////////////////-->

</body>
</html>
